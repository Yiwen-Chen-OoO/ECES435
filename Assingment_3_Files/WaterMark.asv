function [Img_bit] = WaterMark(Original,Watermark,bit_plane)
Result = zeros(size(Original));

for i = 1:8
    if i ~= bit_plane
        Result = Result + 2^(i).*bitget(Original,bit_plane);
    else
        Result = Result + 2^(i)*bitget(Watermark,bit_plane);
    end
end
Img_bit = Result;
end


